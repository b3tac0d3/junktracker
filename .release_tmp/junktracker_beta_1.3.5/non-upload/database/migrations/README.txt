JunkTracker 1.3.5 (beta) — database migrations for live

Back up your database first.

This release has no new SQL migration files beyond prior 1.3 (beta) drops.

If you have not yet applied the migrations from JunkTracker 1.3 (beta), run those
files in order (see junktracker_beta_1.3/non-upload/database/migrations/README.txt).

If those migrations are already applied, no database changes are required for 1.3.5.
